#include "pch.h"
#include <windows.h>
#include <vector>
#include <string>

void SendACinfoToServer(const std::string& reason, const std::string& detail) {
    // Example: Integration with your backend
    // ShellExecuteA(NULL, "open", "https://your-api.com/report?type=" + reason, NULL, NULL, SW_HIDE);
    // you can link it with your backend api
    // can you add send PC info like HWID or Cpu Name and reason etc from here and link it with your api
}
